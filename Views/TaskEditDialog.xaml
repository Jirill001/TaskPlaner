<Window x:Class="TaskPlaner.Views.TaskEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование задачи" 
        Height="400" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Название:" Margin="0,5"/>
        <TextBox Grid.Row="1" x:Name="TitleBox" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Описание:" Margin="0,5"/>
        <TextBox Grid.Row="3" x:Name="DescriptionBox" Height="60" TextWrapping="Wrap"
             AcceptsReturn="True" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Начало:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker x:Name="StartDatePicker" Width="120"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                <TextBlock Text="Конец:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker x:Name="EndDatePicker" Width="120"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="5" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Приоритет:" Margin="0,0,0,5"/>
                <ComboBox x:Name="PriorityComboBox"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Статус:" Margin="0,0,0,5"/>
                <ComboBox x:Name="StatusComboBox"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Прогресс (%):" Margin="0,0,0,5"/>
                <Slider x:Name="ProgressSlider" Minimum="0" Maximum="100" 
                    Value="0" TickFrequency="5" IsSnapToTickEnabled="True"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Цвет задачи:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="ColorComboBox" Width="150"/>
        </StackPanel>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" 
                Margin="0,15,0,0">
            <Button Content="OK" Width="80" Margin="0,0,5,0" Click="OkButton_Click"/>
            <Button Content="Отмена" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>

</Window>